# School Classes & Afterschool Programs Scheduler

Multi-school scheduling app for classes and afterschool programs.

- **Live mockup:** once GitHub Pages is enabled, the clickable mockup is served at the repo's Pages URL (`index.html`).
- **Build plan:** see [docs-build-plan.md](docs-build-plan.md) — 6-phase plan (foundation → admin core → landing → student booking → teacher tools → multi-school polish).

## Repo layout (planned for the real build)

```
index.html            # clickable mockup (Pages site, for stakeholder review)
docs-build-plan.md    # approved build plan
app/                  # Phase 1+: the real application (Next.js) lives here
```
